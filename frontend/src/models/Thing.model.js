export default class Thing {
  constructor(date, time, iduser, username, title, content) {
    this.date=date;
    this.time=time;
    this.iduser=iduser;
    this.username=username;
    this.title=title;
    this.content= content;
  }
}